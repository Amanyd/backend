// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: progress.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const getCourseProgress = `-- name: GetCourseProgress :many
SELECT id, user_id, course_id, is_completed, completed_at FROM user_course_progress
WHERE user_id = $1 AND course_id = $2
`

type GetCourseProgressParams struct {
	UserID   uuid.UUID `json:"user_id"`
	CourseID uuid.UUID `json:"course_id"`
}

func (q *Queries) GetCourseProgress(ctx context.Context, arg GetCourseProgressParams) ([]UserCourseProgress, error) {
	rows, err := q.db.Query(ctx, getCourseProgress, arg.UserID, arg.CourseID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []UserCourseProgress{}
	for rows.Next() {
		var i UserCourseProgress
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.CourseID,
			&i.IsCompleted,
			&i.CompletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getFileProgress = `-- name: GetFileProgress :many
SELECT ufp.id, ufp.user_id, ufp.file_id, ufp.is_viewed, f.lesson_id FROM user_file_progress ufp
JOIN files f ON f.id = ufp.file_id
JOIN lessons l ON l.id = f.lesson_id
WHERE ufp.user_id = $1 AND l.course_id = $2
`

type GetFileProgressParams struct {
	UserID   uuid.UUID `json:"user_id"`
	CourseID uuid.UUID `json:"course_id"`
}

type GetFileProgressRow struct {
	ID       uuid.UUID `json:"id"`
	UserID   uuid.UUID `json:"user_id"`
	FileID   uuid.UUID `json:"file_id"`
	IsViewed bool      `json:"is_viewed"`
	LessonID uuid.UUID `json:"lesson_id"`
}

func (q *Queries) GetFileProgress(ctx context.Context, arg GetFileProgressParams) ([]GetFileProgressRow, error) {
	rows, err := q.db.Query(ctx, getFileProgress, arg.UserID, arg.CourseID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetFileProgressRow{}
	for rows.Next() {
		var i GetFileProgressRow
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.FileID,
			&i.IsViewed,
			&i.LessonID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getLessonProgress = `-- name: GetLessonProgress :many
SELECT ulp.id, ulp.user_id, ulp.lesson_id, ulp.is_completed, ulp.completed_at FROM user_lesson_progress ulp
JOIN lessons l ON l.id = ulp.lesson_id
WHERE ulp.user_id = $1 AND l.course_id = $2
`

type GetLessonProgressParams struct {
	UserID   uuid.UUID `json:"user_id"`
	CourseID uuid.UUID `json:"course_id"`
}

func (q *Queries) GetLessonProgress(ctx context.Context, arg GetLessonProgressParams) ([]UserLessonProgress, error) {
	rows, err := q.db.Query(ctx, getLessonProgress, arg.UserID, arg.CourseID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []UserLessonProgress{}
	for rows.Next() {
		var i UserLessonProgress
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.LessonID,
			&i.IsCompleted,
			&i.CompletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertCourseProgress = `-- name: UpsertCourseProgress :one
INSERT INTO user_course_progress (user_id, course_id, is_completed, completed_at)
VALUES ($1, $2, $3, $4)
ON CONFLICT (user_id, course_id) DO UPDATE SET 
is_completed = $3, completed_at = $4
RETURNING id, user_id, course_id, is_completed, completed_at
`

type UpsertCourseProgressParams struct {
	UserID      uuid.UUID          `json:"user_id"`
	CourseID    uuid.UUID          `json:"course_id"`
	IsCompleted bool               `json:"is_completed"`
	CompletedAt pgtype.Timestamptz `json:"completed_at"`
}

func (q *Queries) UpsertCourseProgress(ctx context.Context, arg UpsertCourseProgressParams) (UserCourseProgress, error) {
	row := q.db.QueryRow(ctx, upsertCourseProgress,
		arg.UserID,
		arg.CourseID,
		arg.IsCompleted,
		arg.CompletedAt,
	)
	var i UserCourseProgress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.CourseID,
		&i.IsCompleted,
		&i.CompletedAt,
	)
	return i, err
}

const upsertFileProgress = `-- name: UpsertFileProgress :one
INSERT INTO user_file_progress (user_id, file_id, is_viewed)
VALUES ($1, $2, $3)
ON CONFLICT (user_id, file_id) DO UPDATE SET is_viewed = $3
RETURNING id, user_id, file_id, is_viewed
`

type UpsertFileProgressParams struct {
	UserID   uuid.UUID `json:"user_id"`
	FileID   uuid.UUID `json:"file_id"`
	IsViewed bool      `json:"is_viewed"`
}

func (q *Queries) UpsertFileProgress(ctx context.Context, arg UpsertFileProgressParams) (UserFileProgress, error) {
	row := q.db.QueryRow(ctx, upsertFileProgress, arg.UserID, arg.FileID, arg.IsViewed)
	var i UserFileProgress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.FileID,
		&i.IsViewed,
	)
	return i, err
}

const upsertLessonProgress = `-- name: UpsertLessonProgress :one
INSERT INTO user_lesson_progress (user_id, lesson_id, is_completed, completed_at)
VALUES ($1, $2, $3, $4)
ON CONFLICT (user_id, lesson_id) DO UPDATE SET 
is_completed = $3, completed_at = $4
RETURNING id, user_id, lesson_id, is_completed, completed_at
`

type UpsertLessonProgressParams struct {
	UserID      uuid.UUID          `json:"user_id"`
	LessonID    uuid.UUID          `json:"lesson_id"`
	IsCompleted bool               `json:"is_completed"`
	CompletedAt pgtype.Timestamptz `json:"completed_at"`
}

func (q *Queries) UpsertLessonProgress(ctx context.Context, arg UpsertLessonProgressParams) (UserLessonProgress, error) {
	row := q.db.QueryRow(ctx, upsertLessonProgress,
		arg.UserID,
		arg.LessonID,
		arg.IsCompleted,
		arg.CompletedAt,
	)
	var i UserLessonProgress
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.LessonID,
		&i.IsCompleted,
		&i.CompletedAt,
	)
	return i, err
}
